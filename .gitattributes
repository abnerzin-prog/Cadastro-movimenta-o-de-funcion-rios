# Mini ERP de Estoque - Sistema de Cadastro de Produtos

# Estrutura para armazenar os produtos (lista de dicionários)
estoque = []

def cadastrar_produto():
    print("\n📦 Cadastro de Produto")
    nome = input("Nome do produto: ")
    categoria = input("Categoria: ")
    preco = float(input("Preço (R$): "))
    quantidade = int(input("Quantidade inicial: "))

    produto = {
        "id": len(estoque) + 1,
        "nome": nome,
        "categoria": categoria,
        "preco": preco,
        "quantidade": quantidade
    }

    estoque.append(produto)
    print(f"✅ Produto '{nome}' cadastrado com sucesso!\n")

def excluir_produto():
    print("\n🗑️ Excluir Produto")
    identificador = input("Digite o ID ou nome do produto que deseja excluir: ")

    for produto in estoque:
        if str(produto["id"]) == identificador or produto["nome"].lower() == identificador.lower():
            estoque.remove(produto)
            print(f"❌ Produto '{produto['nome']}' removido com sucesso!\n")
            return
    print("⚠️ Produto não encontrado.\n")

def relatorio_produtos():
    print("\n📊 Relatório de Produtos Cadastrados")
    if not estoque:
        print("Nenhum produto cadastrado.\n")
        return

    for produto in estoque:
        alerta = "⚠️ ESTOQUE BAIXO" if produto["quantidade"] < 5 else ""
        print(f"ID: {produto['id']} | Nome: {produto['nome']} | "
              f"Categoria: {produto['categoria']} | "
              f"Preço: R$ {produto['preco']:.2f} | "
              f"Quantidade: {produto['quantidade']} {alerta}")
    print()

def menu():
    while True:
        print("====== MENU ERP ESTOQUE ======")
        print("1 - Cadastrar produto")
        print("2 - Excluir produto")
        print("3 - Relatório de produtos")
        print("4 - Sair")
        opcao = input("Escolha uma opção: ")

        if opcao == "1":
            cadastrar_produto()
        elif opcao == "2":
            excluir_produto()
        elif opcao == "3":
            relatorio_produtos()
        elif opcao == "4":
            print("👋 Saindo do sistema... Até logo!")
            break
        else:
            print("⚠️ Opção inválida, tente novamente.\n")

# Início do programa
menu()
